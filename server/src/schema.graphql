type Query {
  test: String!
  getUsers: [Users!]!
}

type Mutation {
  addUser(
    name: String!
  ): Users!
}

type Users {
  id: ID
  name: String
  todos: [Todos!]
}

type Todos {
  id: ID
  text: String
  completed: Boolean
}
